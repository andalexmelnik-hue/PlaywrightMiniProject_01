# This is Playwtight automation mini-poject

## Installation
1. Install python 3.14.0+ version
2. Instal IDE
3. Install all dependencies from Terminal: `pip install .`

## Runs tests
1. Run `pytest` to run all tests in the project